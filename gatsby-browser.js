import './src/styles/global.css';

// gatsby-remark-prismjs only emits the token markup; the colours come from a
// Prism theme stylesheet, which has to be imported explicitly.
import 'prismjs/themes/prism-tomorrow.css';
