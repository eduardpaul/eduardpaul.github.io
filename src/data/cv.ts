// Single import point for the CV data. The file itself stays in content/ so
// it remains editable alongside the blog posts, the way it was under Gatsby.
import cv from '../../content/cv/cv.json';

export default cv;
