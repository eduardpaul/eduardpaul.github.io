{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  // public/ holds verbatim static assets, including a vendored demo app that
  // is not this project's source.
  "exclude": ["dist", "public"]
}
